databases:
  mysql_database:
    host: 127.0.0.1
    port: 3306
    type: mysql
    user: user
    password: password
    database: database
s3:
  default:
    endpoint: "http://localhost:9000"
    region: "eu-central-1"
    accessKeyId: "minioadmin"
    secretAccessKey: "minioadmin"
    pathStyleAccess: true
redis:
  default:
    cluster: false
    host: localhost
    port: 6379
#    user: user
#    password: password
    nodes:
      - host: localhost
        port: 6379
      - host: localhost
        port: 6380
